{
  "name": "ytmusic-urls",
  "version": "1.0.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=neutral --format=esm --outfile=dist/index.js --external:fs --external:http --external:https --external:stream",
    "deploy": "npm run build && wrangler deploy dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12",
    "hono": "^4.9.0"
  }
}
